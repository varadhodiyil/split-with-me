repos:
  - repo: https://github.com/varadhodiyil/split-with-me.git
    # Ruff version.
    rev: api
    hooks:
      # Run the linter.
      - id: ruff
      # Run the formatter.
      - id: ruff-format
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mad.split.with.me',
  appName: 'Split With Me',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
